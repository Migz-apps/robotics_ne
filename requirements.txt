# Python 3.10–3.12 required (MediaPipe legacy face_mesh breaks on 3.13+).
# Virtual env: .env (Python 3.12)
#   py -3.12 -m venv .env
#   .\scripts\install-deps.ps1
#
# Node.js backend (separate): cd backend && npm install
# ESP8266 firmware: use Arduino IDE or PlatformIO (see esp8266/README.md)

# --- Computer vision (mediapipe 0.10.21 pulls opencv-contrib; skip separate opencv-python) ---
# opencv comes via mediapipe dependency

# --- Numerics ---
numpy>=1.24.0,<2.0.0

# --- ArcFace ONNX inference (CPU) ---
onnxruntime>=1.16.0,<2.0.0

# --- FaceMesh 5-point landmarks ---
mediapipe==0.10.21

# --- MQTT vision node publisher ---
paho-mqtt>=1.6.1,<3.0.0

# --- Operational evidence logs (CSV summary / analysis) ---
pandas>=2.0.0,<3.0.0
